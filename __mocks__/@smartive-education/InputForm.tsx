export const InputForm = ({
  name,
  label,
  labelSize = 'default',
  editType,
  required,
  type = 'text',
  placeholder = 'Placeholder',
  errorMessage,
  autoComplete,
  inputValue,
  setInputValue,
  dispatch,
  onPressEnter,
}: any) => {
  return <input />;
};
